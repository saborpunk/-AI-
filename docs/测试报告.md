# 阶段测试报告

## 第三阶段：咨询持久化验收通过（2026-09-09）

环境：现有 MySQL 8.4.9，localhost:3306；Java jar 监听127.0.0.1:8080，Python Uvicorn监听127.0.0.1:8000。此前经历端口拒绝连接及1045认证失败；用户启动服务并明确密码不含反斜线后，更新项目本地配置，连接及初始化成功。下表的数据库不可用验证发生在此前实际断连时。

| 检查 | 实际结果 |
| --- | --- |
| Java 完整回归 | 13项通过，0失败/错误/跳过：原6项、咨询业务规则5项、数据库连接异常接口2项 |
| Python 完整回归 | 9项通过，保留既有两条依赖弃用提示 |
| 真实 Java/Python 启动 | 已分别启动 jar 和 Uvicorn |
| 原 Java↔Python 预览 | smoke-draft.ps1 通过，有无批次及非法问题均按原约定处理 |
| 真实数据库不可用 | 此前 localhost:3306 实际拒绝连接时，database-unavailable 模式通过，创建/历史返回503及结构化错误 |
| JDBC 连接及账号 | check 成功，服务版本8.4.9；本地凭据文件被Git忽略 |
| 初始化 SQL | 实际执行成功；再次执行后原记录保留 |
| 正常保存、草稿落库、修改确认、404、历史分页 | flow 模式通过；原Python快照不被人工编辑覆盖，已确认记录禁止再修改 |
| 非法输入与旧版本 | flow 通过400与409检查，非法字段、路径、分页等被拒绝；损坏JSON由原接口自动测试覆盖 |
| 并发修改 | 两个线程向真实API以同一版本提交不同回复，恰好一个200、一个409，最终版本仅加1 |
| Python 不可用 | 停止项目Python后，python-unavailable模式通过：生成503、咨询未改变、允许人工确认且原草稿仍为空 |
| 应用重启后读回 | 停止并重新启动Java后，readback对比问题、原草稿、人工回复、状态、版本及时间，完整一致 |
| 最终双服务回归 | 重启Java/Python后 smoke.ps1 与 smoke-draft.ps1 均通过 |

数据库异常自动测试替换测试上下文中的 DataSource；业务规则单元测试使用 Mapper 替身。正常 SQL 和持久化另由真实 MySQL API 测试验证，不以替身测试代替落库。合成测试记录明确标记并保留；最新重启对比快照位于忽略的 .tools/persistence-check.json。上述结果证明第三阶段闭环，不代表高并发容量、生产可用性或真实AI正确性已验收。

本轮没有启动、停止或修改 MySQL 服务，没有操作其安装/数据目录。只连接用户指定 localhost:3306；本地连接配置被 Git 忽略。已完成本阶段验证并记录版本，下一阶段等待确认。提交说明：`feat: 完成咨询草稿持久化与人工确认闭环`。

## 第二阶段：单条业务调用链（2026-09-09）

范围：发芽率问题从真实 Java HTTP 入口，经 Python Mock 返回结构化草稿。不涉及数据库、真实 AI 和客户资料。

| 检查 | 结果 |
| --- | --- |
| Python pytest | 9 passed：原健康检查 + 有无批次 + 6 类非法输入；虚假 100% 指令不进入答案 |
| Java Maven verify | 6 tests、0 failures/errors/skipped：健康测试 + 5 项业务测试 |
| Java 参数拦截 | 空/缺失/超长问题、非法批次、损坏 JSON 返回 400，测试上游调用次数为 0 |
| Java 上游结果检查 | 错编号、取消人工审核、损坏 JSON、缺字段、上游500 均返回502 |
| Java 超时 | 上游延迟2.5秒，测试配置读取超时1秒，返回504；只有一次上游调用 |
| 真实双服务 | jar 在8080、Uvicorn 在8000；健康与业务冒烟通过，有无批次都成功 |
| 真实故障 | 停止 Python 后运行 smoke-draft.ps1 -ExpectUnavailable，通过503及错误JSON检查 |

真实端到端脚本只向 Java 发送业务请求；Python 进程日志可见内部草稿接口 POST 200。Java 自动测试中的上游为 JDK HttpServer 测试替身，不能用它单独替代真实端到端验收。

修复：固定 HTTP/1.1 以去除 Uvicorn 协议升级告警；PowerShell 错误体读取兼容处理。修复后重新执行 Java 全部测试并重启服务复测。

现存不阻塞提示：Python 测试依赖仍有两条既有弃用提示；Java 测试依赖 Mockito 动态 agent 提示；测试代码使用的部分 Jackson API 有弃用提示。均未隐藏，也未造成测试失败。没有进行性能、农业事实正确性或生产部署验收。

本阶段提交信息：`feat: 打通发芽率咨询 Java Python Mock 调用链`。

## 2026-09-09 第 1 阶段范围复核

用户明确要求只完成第 1 阶段并等待确认。已恢复 a3a24f4 的骨架代码，撤回尚未提交的数据库依赖、配置、迁移与启动脚本。重新执行 clean verify：Java 1 项通过；pip check 通过、pytest 1 项通过；重新启动 jar 和 Uvicorn 后，smoke.ps1 检查通过。依赖提示与下文一致。

当前仍不依赖 MySQL，不提供咨询或生成接口。阶段结束停止测试服务，不继续开发后续功能。

日期：2026-09-09。范围：MVP v0.1 环境与后端骨架。代码版本：包含本报告的 `feat: 完成 Java Python 项目骨架与启动验证` 提交，可通过 Git 历史定位。

## 环境

Windows x64，Temurin 21.0.12.1，Spring Boot 4.0.8，Maven Wrapper 3.3.4 / Maven 3.9.16，Python 3.12.10。Python 完整依赖见 requirements.txt，直接依赖版本：FastAPI 0.141.1、Pydantic 2.13.5、Uvicorn 0.52.4、pytest 9.1.1、httpx 0.28.1。

## 结果

| 检查 | 结果 | 证据/方法 |
| --- | --- | --- |
| Java 与 Python 版本 | 通过 | 实际运行 java -version 与 Python --version |
| Maven Wrapper | 通过 | mvnw.cmd -version 输出 3.9.16；识别 JDK 21 |
| Java 构建测试 | 通过 | Wrapper verify：BUILD SUCCESS，1 test，0 failures，0 errors，0 skipped |
| 健康与暴露配置 | 通过 | Java 随机端口真实 HTTP：health 返回 200/UP，无 components；env 返回 404 |
| Python 依赖一致性 | 通过 | pip check：No broken requirements found |
| Python 接口测试 | 通过 | pytest：1 passed；核查 mock 标识、OpenAPI、未实现路由 404 |
| Java 可执行 jar | 通过 | 实际启动 8080；修改调试配置后重启复测 |
| Python 服务 | 通过 | Uvicorn 实际启动 8000；GET /health 返回 UP、seed-ai、mock |
| 双服务冒烟 | 通过 | scripts/smoke.ps1 返回 PASS，包含两个健康接口及 Python OpenAPI |
| 环境脚本修复 | 通过 | 本次终端 JAVA_HOME、项目 MAVEN_USER_HOME 正确；DEBUG=false 后没有大量调试日志 |

首次 Java 构建从 Central 下载依赖；最后一次 Wrapper verify 使用已下载的本地缓存（-o），验证可离线重复构建。没有声称在全新机器和所有操作系统上验收。

## 未解决但不阻塞当前骨架的提示

- Python 测试出现两条依赖弃用提示：Starlette 提示未来替换 httpx 测试后端，以及 AnyIO BlockingPortal 别名弃用。本次测试通过，未隐藏提示；后续升级测试依赖时复核。
- Spring Boot 测试依赖 Mockito 发出 JDK 动态 agent 提示，当前 JDK 21 下测试通过；运行 jar 没有测试依赖。后续升级 JDK 或引入 Mock 测试时处理 agent 配置。
- 首次安装依赖需可用网络；本机 Maven 需要代理，pip 使用清华镜像且绕过故障代理。配置方法已记录在 README，未关闭 TLS 验证。

## 未验收范围

数据库持久化、登录、Java→Python 业务调用、草稿审核、前端、真实 AI、Docker/MySQL 运行、压测和计划书 G1 均未完成。本报告只证明骨架可编译、可启动、可测试，不证明完整业务或生产能力。
