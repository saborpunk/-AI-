# 开发记录

## 第二阶段：单条跨服务业务链（2026-09-09）

按用户最新授权，仅实现发芽率咨询草稿预览，不连接数据库、不操作 MySQL、不增加前端和鉴权。先实现 Python 模板并通过 9 项测试，再实现 Java 参数校验和 HTTP 客户端，通过 6 项测试，随后启动两个真实服务进行业务冒烟。

新增依赖只有 Java Validation starter；RestClient 复用已有 Spring Web，Python 无新依赖。模板对有无批次返回不同补充信息，但不声称已经有批次检测数据。结果不入库、不自动发送给客户。

真实运行发现两处问题并修复：JDK 默认尝试 h2c 升级，Uvicorn 输出协议/缺少 WebSocket 库警告，改为明确 HTTP/1.1，无需安装 WebSocket；PowerShell 5 的 ErrorDetails.Message 在部分 HTTP 错误中为空，冒烟脚本增加读取 ResponseStream 的兼容分支。故障演示中停止 Python 后正确得到 503/AI_UNAVAILABLE。

代码核心是 DraftController、PythonDraftClient 和 mock_generator.py；完整文件职责、数据流及五个知识点见第二阶段说明。完成后等待确认，不实施下一阶段。

## 最新范围调整（2026-09-09）

用户进一步明确数据库边界：保留现有 MySQL 服务，只连接 localhost:3306；不移动或修改系统 MySQL 安装目录、数据目录。后续初始化仅通过项目内 SQL 脚本，项目配置只保存在当前目录。此要求覆盖旧记录中的临时实例和目录清理安排；不再操作遗留数据目录。本次仅更新文档，未连接或修改数据库。

用户要求只交付第 1 阶段，之后等待明确确认。已撤回中断前未提交的第 2 阶段代码（数据库依赖、数据源配置、V1 迁移、两个数据库脚本），恢复到已提交的骨架并重新运行验证。

上轮临时 MySQL 实例已停止。其本地初始化文件不属于阶段交付，不上传 Git。安装目录下的 data 目录移动曾被自动审批拒绝，因此未移动、未删除；不能宣称数据库环境已清理完成。原 MySQL84 Windows 服务配置和 D:/mysql_test 数据未修改。后续若需要处理遗留目录，应先核实归属，再确定操作。

## 阶段 0—1：环境与后端骨架（2026-09-09）

范围：固定 MVP 边界；建立 Java/Python 可启动服务、健康接口、测试、构建入口和运行文档。不提前实现数据库和业务模块。

环境：Windows x64；Temurin JDK 21.0.12.1（D:/dev/sdk）；Maven 3.9.16（用户下载的 ZIP，随后生成并验证 Wrapper）；Python 3.12.10；Node 24.20.0、npm 11.19.0 已有但本阶段不使用。Docker 未验证可用，下一阶段处理。

| 问题 | 原因/证据 | 解决与边界 |
| --- | --- | --- |
| 工具下载中断 | 上一轮出现 EOF、证书吊销服务不可达 | 用户完成下载，复用本地安装包；没有把残缺 ZIP 当安装完成 |
| Python 环境混乱 | 默认 python 指向 3.14，py 曾找不到安装 | 显式使用已安装的 3.12 创建项目 .venv，不改动其他版本 |
| pip 下载失败 | SSL EOF 和 ProxyError | 仅当前安装进程 NO_PROXY=*，使用清华 HTTPS 镜像成功下载 |
| Maven TLS 握手失败 | Central 连接终止 | 当前命令显式使用用户已配置的 127.0.0.1:7897 代理，仍用 Central；不持久修改网络配置 |
| Maven 把版本当构建阶段 | 未加引号的 -Dmaven=3.9.16 被 PowerShell 拆分 | 对完整 -D 参数加引号，Wrapper 生成成功 |
| 环境脚本项目路径为空 | Windows PowerShell 读取无 BOM 中文注释出现兼容问题 | 执行脚本使用 ASCII 注释，中文说明保留在 README，复测环境变量正确 |
| Spring 输出大量 DEBUG 日志 | 环境中 DEBUG=release 被配置系统读取，环境变量优先于 application.yml | 仅在项目终端设置 DEBUG=false；不能只改 YAML 就宣称解决 |

核心代码与学习点见面试学习笔记；实际检查结果见测试报告。依赖安装只在项目虚拟环境和缓存目录内进行。Maven Wrapper 首次验证曾使用默认用户缓存，修复环境脚本后改为项目 .m2 缓存。

下一阶段：检查 Docker/MySQL 8.4 运行条件，引入迁移和最小咨询创建、查询；必须在真实数据库上验证，再进入模拟草稿生成。
