# 项目长期协作说明

后续工作先读README、docs/architecture.md及本文件。最新用户指令优先于历史路线。

## 方向与阶段

先把传统Java Web业务做完整，再引入AI：V1 CRUD → V2持久层/用户表 → V3 JWT与前后端基础 → V4完整传统业务 → V5 Spring AI/模型 → V6多轮消息与对话评估 → V7 SSE。
当前完成V2持久层/MyBatis-Plus、用户表与可空会话归属；V3认证阶段等待用户确认。客户/商家两个角色在认证阶段实现，不同时引入另一套角色。
不提前添加Redis、MQ、Nacos、微服务、缓存、向量库、限流熔断等；整体业务完成且有真实需求后再提醒评估。

## 实施与说明

逐阶段实现、自动测试、真实启动、API与异常测试、修复回归，再提交。沿用可复用代码，不破坏旧数据。
用户是初学者，沟通用简短具体说明；不生成学习教程或冗长知识清单。关键注释解释业务规则与设计原因。新增依赖前说明用途。
当前使用MyBatis-Plus Boot4 Starter；基础读取用BaseMapper，分页及version更新保留显式SQL。用户内部创建只存BCrypt哈希，不开放无认证的用户管理或归属修改接口。新传统业务不依赖AI/Python，旧生成接口仅作兼容。

## MySQL边界

仅连接现有localhost:3306。不得移动、修改、删除MySQL安装目录或数据目录，不创建替代实例，不改端口或服务配置。
项目配置只存项目目录；认证资料写Git忽略的本地配置，不打印、不提交。
初始化或结构变更用可审阅项目SQL，不操作物理数据文件。数据库故障优先在测试中模拟，不停止真实MySQL。

## 文档

只维护README、docs/architecture.md、docs/schema.md、docs/api.md、docs/development.md。架构记录阶段路线；启动说明附简短测试证据。不要新增阶段文档、学习教程或重复历史说明。旧内容可从Git历史查看。

## 版本管理

每个完成且通过必要验证的版本创建新Git commit，并推送用户指定仓库https://github.com/saborpunk/-AI-.git。
只提交相关源码、SQL和工程文档，不提交凭据、真实客户数据或构建产物。不覆盖旧提交、不强制推送。
“历史申请”按commit/push执行；若用户明确要求Pull Request，再采用相应流程。
