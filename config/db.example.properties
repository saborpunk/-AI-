# Copy to config/db.local.properties and fill in the existing MySQL account.
# Use an account authorized for the project schemas only. Never commit credentials.
spring.datasource.username=REPLACE_WITH_USERNAME
spring.datasource.password=REPLACE_WITH_PASSWORD
